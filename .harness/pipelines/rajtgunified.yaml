pipeline:
  stages:
    - name: Stage
      id: stage
      steps:
        - name: Step
          id: step
          run: echo "hello world"
        - name: rajTG
          id: rajtg
          iacm:
            uses: terragruntStep@1.0.3
            with:
              command: init
              connectorRef: account.rajdocker
              image: harnessdev/dev:265
  inputs:
    workspace:
      type: string
      default: rajTGUnified
