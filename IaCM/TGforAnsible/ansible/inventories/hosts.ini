[dev]
# Populate with IPs from: cd ../environments/dev/ec2 && terragrunt output instance_public_ip
# dev-instance ansible_host=<DEV_IP>

[prod]
# Populate with IPs from: cd ../environments/prod/ec2 && terragrunt output instance_public_ip
# prod-instance ansible_host=<PROD_IP>

[all:vars]
ansible_user=ec2-user
ansible_connection=ssh
